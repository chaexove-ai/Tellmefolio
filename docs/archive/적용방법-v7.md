# v7 — 빈 이미지 자리를 그라디언트 그레인 커버로

v6까지 적용된 상태에서 덮어쓰면 됩니다.

```
src/components/GrainCover.tsx        ← 신규
src/index.css                        ← 덮어쓰기 (.grain-cover 추가)
src/pages/Landing.tsx                ← 덮어쓰기 (갤러리 카드에 커버)
src/pages/VersionHistory.tsx         ← 덮어쓰기
src/pages/gallery/Gallery.tsx        ← 덮어쓰기
src/pages/gallery/GalleryDetail.tsx  ← 덮어쓰기
src/pages/wizard/TemplateStyle.tsx   ← 덮어쓰기
```

```bash
npm run build && npm run dev
```

---

## 채운 자리 (전수 조사했습니다)

소스 전체를 훑어서 빈 이미지·미리보기 영역을 다 찾았습니다.

| 화면 | 자리 | 기존 |
|---|---|---|
| `/gallery` | 카드 썸네일 6개 | `ImageOff` 아이콘 + 회색 박스 |
| `/gallery/:id` | 대표 이미지 | 빈 테두리 + "포트폴리오 대표 이미지" |
| `/gallery/:id` | 케이스 스터디 영역 | 빈 테두리 + 안내 문구 |
| `/library/portfolios/:id/versions` | 현재 작업본 미리보기 | 빈 테두리 |
| 〃 | 선택 버전 미리보기 | 빈 테두리 |
| `/wizard/style` | 실시간 미리보기 | 빈 테두리 |
| `/wizard/style` | 템플릿 4개 | **썸네일 없음** (이름만 나열) |
| 랜딩 | 갤러리 미리보기 카드 | 이미지 없음 |

템플릿 4개는 원래 빈 자리도 아니었는데, 이름만 나열돼 있어서
"무엇이 다른지"가 전혀 보이지 않길래 같이 넣었습니다.

---

## 어떻게 만들었나

**이미지 파일이 하나도 없습니다.** CSS 그라디언트 + SVG 노이즈뿐입니다.

- 전송량 0 — 썸네일 8장을 넣어도 네트워크 요청이 늘지 않습니다
- 어떤 크기에서도 깨지지 않습니다 (벡터)
- 라이트/다크 테마와 무관하게 동일하게 보입니다 (이건 "그림"이니까요)

구조는 이렇습니다.

```
크림 바탕 (hsl 34~46, 48%, 95%)
  + 살구·앰버·연노랑·복숭아 블롭 4개
    (요소보다 크게 잡아 가장자리 밖으로 번지게)
  + 그레인 2겹
    - 굵은 결   : multiply, 0.42
    - 미세 알갱이: screen,   0.22  ← 필름 느낌
```

**seed 로 결정됩니다.** `GrainCover seed={g.id}` 처럼 넘기면
색조·위치·크기가 그 문자열에서 계산됩니다. 그래서

- 같은 포트폴리오는 언제 봐도 **같은 커버**를 갖습니다
- 목록의 카드들은 **전부 다른 그림**이 됩니다
- 갤러리 목록의 카드와 상세 페이지 대표 이미지가 **같은 그림으로 이어집니다**
- 템플릿을 바꾸면 미리보기 커버도 함께 바뀝니다 —
  선택이 결과에 반영된다는 감각이 생깁니다
- 버전 관리에서 두 미리보기가 서로 다른 그림이라
  "다른 버전을 보고 있다"가 눈으로 구분됩니다

---

## 쓰는 법

```tsx
<GrainCover seed={item.id} className="aspect-[4/3] rounded-xl mb-3" />

// 라벨을 얹을 때
<GrainCover seed={item.id} className="h-48 rounded-2xl">
  <span className="grain-cover-label">포트폴리오 대표 이미지</span>
</GrainCover>
```

크기·모서리는 `className` 으로만 정합니다. 컴포넌트는 그림만 그립니다.

## 조절 지점

| 무엇 | 어디 |
|---|---|
| 색조 범위 (지금은 살구~연노랑) | `GrainCover.tsx` 의 `H` 배열 |
| 블롭 크기 | 같은 파일 `W` / `V` 배열 |
| 그레인 세기 | `index.css` 의 `.grain-cover::before / ::after` opacity |
| 바탕 밝기 | `.grain-cover` 의 `background-color` 마지막 값 (95%) |

---

## 나중에 실제 썸네일이 생기면

`<img>` 를 앞에 두고 이 컴포넌트를 뒤에 깔면 그대로 폴백이 됩니다.
이미지가 없거나 로딩 중일 때 회색 박스 대신 이 커버가 보입니다.
