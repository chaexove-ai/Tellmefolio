# v6 — 차별점 스크롤 몰입 구간

v5까지 적용된 상태에서 덮어쓰면 됩니다.

```
src/components/PerspectiveScroller.tsx  ← 신규
src/components/BeforeAfterDemo.tsx      ← 변경 없음(폴백용, 확인 겸 동봉)
src/pages/Landing.tsx                   ← 덮어쓰기
```

```bash
npm run build && npm run dev
```

---

## 무엇이 바뀌나

기존에는 차별점이 **탭 위젯**이었습니다. 눌러야 비교가 되는데,
랜딩 방문자 대부분은 누르지 않고 지나갑니다. 그래서 이 서비스의 전부인
"같은 경험을 여러 직무 언어로"가 전달되지 않았습니다.

이제 **스크롤 동작 자체가 직무 전환**입니다.

```
구간 진입   →  화면이 살짝 확대되며 선명해짐 (blur 4px → 0, scale 0.94 → 1)
              동시에 화면이 잠깁니다(sticky)
스크롤 ↓    →  원본 메모는 그대로 남고, 아래 결과만 다시 쓰입니다
              프론트엔드 → UX → 서비스 기획
              진행 막대 3개가 차례로 채워집니다
구간 종료   →  흐려지며 다음 섹션으로 넘어갑니다
```

원본 메모를 화면에 계속 남겨둔 것이 핵심입니다.
"입력은 하나인데 출력만 바뀐다"가 눈으로 확인됩니다.

---

## 구현 메모

### GSAP pin 대신 `position: sticky`

ScrollTrigger 의 `pin` 은 스페이서 DOM 을 주입하고 레이아웃을 다시 계산합니다.
SPA 라우팅과 지연 로드되는 웹폰트가 겹치면 위치가 어긋나기 쉽습니다
(앞서 리빌이 늦게 터지던 것과 같은 계열의 문제입니다).

바깥 래퍼에 높이를 주고 안쪽을 `sticky top-0 h-screen` 으로 붙이면
브라우저가 알아서 처리합니다. 진행도만 스크롤 위치로 계산하면 되고,
`ScrollTrigger.refresh()` 에 의존하지 않습니다.

프레임마다 `setState` 하지 않도록, 막대 너비와 transform 은 ref 로
DOM 에 직접 씁니다. 관점 인덱스가 바뀔 때만 리렌더합니다.

### 자동으로 빠져나가는 경우

| 환경 | 동작 |
|---|---|
| 화면 너비 < 768px | 몰입 구간 없이 **기존 탭 위젯**으로 |
| `prefers-reduced-motion` | 동일하게 탭 위젯으로 |

작은 화면에서 화면이 잠기면 답답하고, 스크롤 잠금은 전정기관에 부담을 줍니다.
창 크기를 바꾸거나 시스템 설정을 바꾸면 실시간으로 전환됩니다.

### 접근성

세 관점을 모두 DOM 에 두고 시각적으로만 전환합니다.
스크린리더는 세 관점을 순서대로 다 읽으므로 정보 손실이 없고,
패널 안에 포커스 가능한 요소가 없어 숨은 초점 문제도 생기지 않습니다.

---

## 조절 지점

| 무엇 | 어디 | 지금 값 |
|---|---|---|
| 관점 하나당 스크롤 길이 | `PerspectiveScroller.tsx` 의 `VH_PER_VIEW` | `1` (뷰포트 1개 높이) |
| 빨려들어가는 정도 | 같은 파일 `0.94 + 0.06 * enter` | 6% 확대 |
| 흐림 강도 | 같은 파일 `(1 - enter) * 4` | 최대 4px |
| 몰입 해제 기준 너비 | 같은 파일 `(min-width: 768px)` | 768px |

**너무 길게 느껴지면 `VH_PER_VIEW` 를 `0.8` 정도로 낮춰보세요.**
관점이 3개라 현재는 이 구간에서만 화면 3개 높이만큼 스크롤해야 넘어갑니다.
이게 이번 변경의 가장 큰 트레이드오프입니다 — 차별점은 확실히 각인되지만,
바쁜 방문자에겐 길게 느껴질 수 있습니다. 실제로 스크롤해 보시고 판단해 주세요.
