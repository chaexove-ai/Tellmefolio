# v4 — 히어로 모션 A(문장 재작성) + 책장 인터랙션

v3까지 적용된 상태에서 덮어쓰면 됩니다.

```
src/landingContent.ts            ← 덮어쓰기 (heroRewrite 추가)
src/components/HeroRewrite.tsx   ← 신규
src/components/Bookshelf.tsx     ← 덮어쓰기
src/pages/Landing.tsx            ← 덮어쓰기 (HeroGlobe → HeroRewrite)
src/index.css                    ← 덮어쓰기 (.shelf / .book-spine / .book-title 추가)
```

지구본 제거:

```bash
grep -rn "HeroGlobe" src/        # 결과가 비어야 안전
rm src/components/HeroGlobe.tsx
npm uninstall cobe
npm run build && npm run dev
```

---

## 1. 히어로 모션 — 문장 재작성

원본 메모 한 줄이 직무별로 다시 쓰이는 과정을 타이핑으로 보여줍니다.
프론트엔드 → UX → 기획 순으로 순환합니다.

- **WebGL이 사라집니다.** `cobe` 제거 → 번들 감소 + 캔버스 4배 오버드로 문제 소멸
- 접근성: 한 글자씩 바뀌는 텍스트를 스크린리더가 읽으면 소음이라
  타이핑 영역은 `aria-hidden`, 세 버전을 한 번에 읽는 `sr-only` 블록을 별도 제공
- `prefers-reduced-motion` → 타이핑 없이 첫 문장만 정적 표시
- 백그라운드 탭에서는 루프 정지

속도 조절은 `HeroRewrite.tsx` 상단 상수 세 개:

```ts
const TYPE_MS = 32;   // 한 글자 간격
const HOLD_MS = 2400; // 다 쓰고 머무는 시간
const ERASE_MS = 12;  // 지우는 속도
```

---

## 2. 책장 — 잘림 문제 해결

첫 시안에서 지적하신 잘림을 포함해 세 가지를 고쳤습니다.
브라우저에서 직접 렌더링해 확인했습니다.

### ① 제목이 컨테이너에 잘리던 문제

가로 스크롤 컨테이너는 CSS 규칙상 세로도 함께 자릅니다
(`overflow-x: auto` 를 주면 `overflow-y: visible` 이 불가능합니다).
말풍선을 책 위에 절대배치하는 방식은 구조적으로 잘릴 수밖에 없었습니다.

→ **제목을 스크롤 영역 바깥 표시줄로 옮겼습니다.** 커서를 올린 책의 제목이
그 줄에 뜹니다. 위치가 고정돼 있어 눈이 따라다닐 필요도 없어졌습니다.

### ② 책 아랫부분이 잘리던 문제

`perspective-origin` 이 `50% 85%` 였습니다. 소실점보다 아래에 있는 지점은
앞으로 당길 때 아래로 밀려나므로, 책의 밑동이 선반 아래로 삐져나가 잘렸습니다.

→ **소실점을 선반 바닥(`50% 100%`)으로** 옮겼습니다. 이제 책이 위로만 커집니다.
위쪽은 `pt-10` 여유로 받습니다. `rotateX` 도 제거했습니다(밑동을 밀어내던 원인).

### ③ 툴팁 두 개가 겹치던 문제

`title` 속성 때문에 브라우저 기본 툴팁이 커스텀 라벨과 같이 떴습니다.
→ `title` 제거.

### ④ 세로 제목이 여러 칸으로 접히던 문제

긴 제목이 두세 칸으로 접히면서 읽는 순서가 깨졌습니다.
→ `white-space: nowrap` + `text-overflow: ellipsis` 로 한 칸 고정, 넘치면 말줄임.
전체 제목은 위 표시줄에서 확인됩니다.

### 조절 방법

`src/index.css` 의 `.book-spine:hover` 한 곳만 만지면 됩니다.

```css
transform: translateY(-14px) translateZ(44px) rotateY(-24deg);
```

- 더 많이 빠져나오게 → `translateZ(44px)` 를 키우기 (단, 위쪽 `pt-10` 도 함께)
- 덜 틀어지게 → `rotateY(-24deg)` → `-14deg`
- 더 높이 들리게 → `translateY(-14px)` 를 키우기

### 남은 트레이드오프

책등은 세로쓰기라 훑어보기는 표지형보다 불리합니다.
대신 폭이 104px → 38px 이라 한 화면에 3배 가까이 들어가고,
제목 표시줄이 전체 제목을 받쳐줍니다.
아니다 싶으시면 git 히스토리에서 이전 `Bookshelf.tsx` 를 복원하시면 됩니다.
