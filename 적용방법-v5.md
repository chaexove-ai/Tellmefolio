# v5 — 랜딩 리듬 재설계 + 스크롤 모션

v4까지 적용된 상태에서 덮어쓰면 됩니다.

```
src/lib/scrollRefresh.ts            ← 신규 (ScrollTrigger 재계산 공용 유틸)
src/components/ScrollProgress.tsx   ← 신규 (상단 진행 바)
src/components/Steps.tsx            ← 신규 (3단계 세로 연결선)
src/components/Reveal.tsx           ← 덮어쓰기 (공용 유틸 사용하도록 정리)
src/pages/Landing.tsx               ← 덮어쓰기 (리듬 재설계)
src/index.css                       ← 덮어쓰기 (섹션 리듬 클래스 추가)
```

```bash
npm run build && npm run dev
```

---

## 진단: 단조로움의 원인은 모션이 아니었습니다

기존 랜딩을 다시 보니 원인이 명확했습니다.

- **3열 카드 그리드가 연속 세 번** — 3단계 / 갤러리 / 대상
- **섹션 제목이 전부 동일** — 모두 `text-sm text-neutral-500`
- **여백도 균일** — 전부 `py-16`
- **배경도 하나** — 처음부터 끝까지 같은 면

같은 형태가 반복되니 스크롤해도 "섹션이 바뀌었다"는 신호가 없었습니다.
여기에 스크롤 모션만 얹으면 **똑같은 블록이 똑같이 애니메이션되는** 상태가 됩니다.
구분은 여전히 안 되고 산만함만 늘어납니다.

솔직히 이건 제가 v3에서 갤러리와 3단계를 둘 다 같은 카드 그리드로 찍어내면서
만든 문제이기도 합니다.

그래서 **리듬을 먼저 잡고 그 위에 모션을 얹었습니다.**

---

## ① 배경 면 교차

섹션마다 기본 면과 밝은 면(`.surface-alt`)을 번갈아 씁니다.
위아래 경계선이 생기면서 섹션이 물리적으로 나뉩니다.

```
히어로        기본
차별점        밝은 면  ← 경계
작동 방식     기본     ← 경계
결과물        밝은 면  ← 경계
대상          기본     ← 경계
FAQ           밝은 면  ← 경계
최종 CTA      기본
```

## ② 섹션 제목에 위계

```
.sec-eyebrow   작은 대문자 라벨 (브랜드 컬러)   ← 신설
.sec-title     Gowun Batang 22px               ← 기존 14px 회색에서 승격
.sec-sub       설명 한 줄                       ← 신설
```

기존에는 "핵심 기능" 같은 제목이 본문보다도 작고 흐렸습니다.

## ③ 레이아웃 형태를 섹션마다 다르게

| 섹션 | 기존 | 변경 |
|---|---|---|
| 작동 방식 | 3열 카드 | **세로 연결선 + 마디** — 순서가 있는 정보라 의미에 맞습니다 |
| 결과물 | 3열 카드 | **가로 스크롤** — 잘린 카드가 "더 있다"를 보여줍니다. 6개로 늘렸습니다 |
| 대상 | 3열 카드 | **정의 목록(dl)** — 짧은 텍스트라 카드가 과했습니다 |

카드 그리드가 세 번 → 0번이 됐습니다.

## ④ 스크롤 모션 3가지 (절제된 강도)

셋 다 역할이 분명한 것만 남겼습니다.

**진행 바** — 최상단 2px. 랜딩이 길어져서 "지금 어디쯤인지" 감이 없어졌습니다.
읽기를 방해하지 않는 선에서 가장 값싼 방향 감각입니다.

**3단계 연결선** — 스크롤에 따라 선이 그어지고 마디가 순서대로 켜집니다.
장식이 아니라 "순서"라는 의미를 그대로 표현합니다.

**히어로 전환** — 스크롤하면 히어로가 살짝 흐려지고 축소되며 다음 섹션에
자리를 내줍니다. 장면이 바뀌었다는 신호입니다.

> 카드가 아래에서 올라오는 리빌(`Reveal`)은 이미 있습니다.
> 여기에 더 얹으면 산만해지기만 해서 3개로 제한했습니다.

모두 `prefers-reduced-motion` 에서 꺼지거나 정적 상태로 대체됩니다.

---

## 부수 정리

`ScrollTrigger.refresh()` 를 여러 컴포넌트가 각자 호출하던 것을
`src/lib/scrollRefresh.ts` 공용 유틸로 합쳤습니다.
웹폰트 로드 후 딱 한 번만 실행되고, 몇 번 호출하든 중복되지 않습니다.

## 조절 지점

| 무엇 | 어디 |
|---|---|
| 밝은 면의 밝기 | `index.css` 의 `.surface-alt` 그라데이션 값 |
| 진행 바 두께·색 | `ScrollProgress.tsx` 의 `h-[2px] bg-brand` |
| 연결선 그리는 타이밍 | `Steps.tsx` 의 `start: "top 70%"` / `end: "bottom 75%"` |
| 히어로 흐려지는 정도 | `Landing.tsx` 의 `opacity: 0.28, scale: 0.975` |
| 섹션 간 여백 | 각 `<section>` 의 `py-24` |
